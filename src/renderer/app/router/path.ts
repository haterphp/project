const PREFIX = ''

enum RouterPath {
    UF_GREETING = `/`,
    UF_UPLOAD = `/upload`,
    UF_RESULTS = `/results/:id`,

    ABOUT_APP = '/about/app',
    ABOUT_DEV = '/about/dev',
}

export { RouterPath, PREFIX }